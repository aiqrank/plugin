{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "timeout": 2,
            "command": "PY=$(command -v python3 || command -v python); [ -n \"$PY\" ] && \"$PY\" -u \"${CLAUDE_PLUGIN_ROOT}/scripts/hook_nudge_if_stale.py\" || true"
          },
          {
            "type": "command",
            "shell": "bash",
            "timeout": 60,
            "command": "PY=$(command -v python3 || command -v python); [ -n \"$PY\" ] && AIQRANK_HOOK=session_start \"$PY\" -u \"${CLAUDE_PLUGIN_ROOT}/scripts/hook_upload_today.py\" || true"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "timeout": 60,
            "command": "PY=$(command -v python3 || command -v python); [ -n \"$PY\" ] && AIQRANK_HOOK=session_end \"$PY\" -u \"${CLAUDE_PLUGIN_ROOT}/scripts/hook_upload_today.py\" || true"
          }
        ]
      }
    ]
  }
}
